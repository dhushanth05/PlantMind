# Neo4j Infrastructure

Purpose: Neo4j setup assets.

Responsibilities: graph constraints, indexes, import files, and Cypher migration scripts.

Future services: APOC-backed migrations, knowledge graph constraint validation, and graph seed datasets.

