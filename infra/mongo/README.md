# MongoDB Infrastructure

Purpose: MongoDB and MongoDB Atlas setup assets.

Responsibilities: database initialization scripts, collection indexes, vector search index definitions, and seed data boundaries.

Future services: Atlas vector search indexes, migration scripts, and tenant bootstrap automation.

