# MongoDB Init Scripts

Purpose: optional local MongoDB initialization scripts mounted into the Mongo container.

Responsibilities: keep local-only seed or index bootstrap scripts separate from application code.

Future services: collection creation, Atlas-compatible vector index documentation, tenant seed data, and development fixtures.

