# Layout Components

Purpose: shared application shell pieces such as side navigation, top bars, breadcrumbs, page headers, and responsive layout primitives.

Responsibilities: keep page composition consistent across PlantMind modules without owning domain data or feature-specific behavior.

Future services: workspace switcher, RBAC-aware navigation, command palette entrypoints, notification rail, and global search shell.

